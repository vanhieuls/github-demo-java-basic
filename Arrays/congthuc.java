import java.util.*;
import java.util.Scanner;

public class congthuc {
    public static void main(String[] args) {
        Scanner scan = new Scanner(System.in);
        // int[] a = new int[10];
        int a[] = { 1, 2, 3, 4, 5, 6 };
        /*******
         * SORT
         * Sort tang dan
         * Arrays.sort(a);
         * sort gian dan, luu y phai dua kieu du lieu ve dang Interger, Double,....
         * Arrays.sort(a,Collections.reverseOrder());
         */
        /* 
        binary Search (chi ap dung cho mang tang dan), có hỗ trợ tìm kiếm trong đoan, vidu int res=Arrays.binarySearch(a,1,3,4); tìm 4 trong đoanj [1,2]
        int res = Arrays.binarySearch(a, 0);//logN
        (-InsertionIndex+1)
         * System.out.println(res);
        */
        
        /* Muon gan gia tri trong mang thanh gia tri x, ta dùng Arrays.fill(a,x) hoác trong doan Arrays.fill(a,0,9,x) */ 
        
        //for (int x : a)
          //  System.out.println(x);

    }
}
